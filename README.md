# Veridian Auth 🌿
A modern, full-stack User Management System built with the MEN Stack (MongoDB/SQLite, Express, Node.js). This project features a high-end Dark Green "Glassmorphism" aesthetic, providing a secure and seamless experience for user authentication and community management.

## ✨ Key Features
* Secure Authentication: Full registration and login flow with encrypted password handling (Bcrypt).

* Modern UI/UX: A sleek "Dark Mode" design using Bootstrap 5, custom CSS gradients, and glassmorphism effects.

* User Dashboard: Personalized user area displaying account details and membership stats.

* Security Controls: Real-time password updates via AJAX-powered modals without page refreshes.

* Community Directory: A stylized list of all registered users featuring a "Self-ID" badge system.

* Responsive Design: Fully optimized for mobile, tablet, and desktop views.

## 🚀 Tech Stack
* Frontend: EJS (Embedded JavaScript), Bootstrap 5, Bootstrap Icons, AJAX (Fetch API).

* Backend: Node.js, Express.js.

* Database: SQLite (Local) / Compatible with MongoDB.

## 🛠️ Installation & Setup
1. Clone the repository:
   ```
   git clone https://github.com/Adnan-Deuser/Login_User_Management_MEN.git
   cd Login_User_Management_MEN
   ```
2. Install dependencies:
   ```
   npm install
   ```
3. Run the Application
```
npm run dev
```
## 📁 Project Structure
```
├── models/         # Database Schemas (User)
├── public/         # Static assets (CSS, Images)
├── routes/         # Express Route Handlers
├── views/          # EJS Templates (Dashboard, Login, Register)
├── app.js          # Entry point of the application
└── database.sqlite # Local SQLite database file
```
